# PaymentProcessingService
A Payment Processing API for an e-commerce platform.

Clients (other services) will call your API to:
a) Create a payment
b) Confirm a payment
c) Retrieve payment status

The system **must be safe against duplicate requests** and handle concurrency correctly.
